spring.application.name=bets-ms
server.port=8200

# Ribbon balancer
#mgr-ms.ribbon.listOfServers=localhost:8100,localhost:8101
#usr-ms.ribbon.listOfServers=localhost:8000,localhost:8001

# Config server
spring.cloud.config.uri=http://localhost:8888

# Config profile
spring.profiles.active=dev
